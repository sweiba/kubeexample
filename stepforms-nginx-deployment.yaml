apiVersion: apps/v1
kind: Deployment
metadata:
  name: stepforms-nginx
  labels:
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stepforms-nginx
      tier: backend
  template:
    metadata:
      labels:
        app: stepforms-nginx
        tier: backend
    spec:
      volumes:
        - name: config
          configMap:
            name: stepforms-nginx-config
            items:
              - key: config
                path: site.conf
      containers:
        - name: nginx
          image: nginx
          ports:
            - containerPort: 80
